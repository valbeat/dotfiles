# orca-automations stub (CI only)

CI replaces the private `orca-automations` flake input with this directory
(`--override-input`), so the public build check needs no credentials.
The real definitions and their tests live in valbeat/orca-automations.
