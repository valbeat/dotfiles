language: bash
os:
  - linux

services:
  - docker

git:
  submodules: false

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive
  - docker build -t dotfiles-test .

script:
  - docker run --rm dotfiles-test make test
