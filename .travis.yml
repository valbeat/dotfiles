language: c
git:
  submodules: false

os:
  - linux

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive

install:
  - make install

script:
  - make --silent test
